const initialState = {
  items: [],
};

function shoppingCartReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export { shoppingCartReducer, initialState };
